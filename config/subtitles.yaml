# Subtitle System Configuration
# Complete subtitle system including ASS effects and positioning

subtitle_settings:
  # Whether to generate and embed subtitles in the video
  enabled: true

  # ---- UNIFIED POSITIONING ----
  # Content-aware positioning that automatically adapts to visual layout

  # POSITIONING MODES:
  # - Absolute Mode: Fixed positioning (content_aware=false)
  #   anchor + margin with no content awareness
  #   Example: anchor="bottom", margin=0.1, content_aware=false
  #
  # - Relative Mode: Dynamic positioning (content_aware=true)
  #   Adjusts position based on visual content boundaries
  #   Example: anchor="below_content", margin=0.08, content_aware=true

  # Anchor point for subtitle positioning
  # Options: top, center, bottom, above_content, below_content
  anchor: "below_content"

  # Margin as fraction of frame height (0.0-0.5)
  margin: 0.08

  # Enable content-aware positioning (adjusts based on visual elements)
  # true = Relative Mode, false = Absolute Mode
  content_aware: true

  # ---- STYLING ----
  # Style preset determines overall appearance
  # Available presets: minimal, modern, bold, animated, random
  # - minimal: Clean, simple styling with no effects
  # - modern: Contemporary look with karaoke effect
  # - bold: High contrast with fade effect
  # - animated: Full animations with movement effect
  # - random: Randomized font, colors, and one random effect
  style_preset: "animated"

  # Font size scale factor (0.5-2.0)
  font_size_scale: 1.0

  # Text alignment: left, center, right
  horizontal_alignment: "center"

  # ---- TEXT FORMATTING ----
  # Maximum characters per subtitle line
  max_line_length: 30

  # Maximum duration for any subtitle segment (seconds)
  max_duration: 4.5

  # Minimum duration for any subtitle segment (seconds)
  min_duration: 0.4

  # Probability of applying random animation effects (0.0-1.0)
  animation_probability: 0.3

  # ---- RANDOMIZATION OPTIONS ----
  # Use random color combinations from preset palette
  randomize_colors: false

  # Use random animation effects when available
  randomize_effects: true

  # ---- OUTPUT FORMAT ----
  # Subtitle file format: "srt" or "ass"
  subtitle_format: "ass"

  # ---- LEGACY COMPATIBILITY SETTINGS ----
  # Font settings
  font_directory: "static/fonts"
  font_name: "Gabarito"
  font_size_percent: 0.2
  font_width_to_height_ratio: 0.55

  # Color settings
  font_color: "&H00FFFFFF"
  outline_color: "&HFF000000"
  back_color: "&H99000000"

  # Visual styling
  bold: true
  outline_thickness: 1
  shadow: true

  # Script fallback paths
  script_paths: ["info/script.txt"]

  # Quality thresholds
  subtitle_similarity_threshold: 0.70
  subtitle_overlap_threshold: 65.0
  word_timestamp_pause_threshold: 0.4
  subtitle_split_on_punctuation: true
  punctuation_marks: [".", "!", "?", ";", ":", ","]

# TTS Configuration
tts_config:
  # Order of TTS providers to try
  provider_order: ["google_cloud", "coqui"]

  # Google Cloud Text-to-Speech Settings
  google_cloud:
    model_name: "en-US-Chirp3-HD"
    language_code: "en-US"

    # Voice selection criteria
    voice_selection_criteria:
      - { language_code: "en-US", name_contains: "Chirp3", ssml_gender: "FEMALE" }
      - { language_code: "en-US", name_contains: "Chirp3", ssml_gender: "MALE" }
      - { language_code: "en-US", name_contains: "Chirp", ssml_gender: "FEMALE" }
      - { language_code: "en-US", name_contains: "Chirp", ssml_gender: "MALE" }
      - { language_code: "en-US", name_contains: "Neural2", ssml_gender: "FEMALE" }
      - { language_code: "en-US", name_contains: "Neural2", ssml_gender: "MALE" }
      - { language_code: "en-US", ssml_gender: "FEMALE" }
      - { language_code: "en-US", ssml_gender: "MALE" }

    # Speech parameters
    speaking_rate: 1.0
    pitch: 0.0
    volume_gain_db: 0.0

    # API settings
    debug: false
    api_timeout_sec: 60
    api_max_retries: 2
    api_retry_delay_sec: 5
    last_word_buffer_sec: 0.3

  # Coqui TTS Settings (Local Processing)
  coqui:
    model_name: "tts_models/en/ljspeech/vits"
    speaker_name: null

# ============================================================================
# SUBTITLE EFFECTS (ASS Format Animations)
# ============================================================================
subtitle_effects:
  # Karaoke timing parameters
  karaoke_timing_min_ms: 20
  karaoke_timing_max_ms: 200

  # Animation duration factors (multiplied by segment duration)
  pulse_duration_factor: 500
  bounce_duration_factor: 300
  glow_duration_factor: 400

  # Scale effect parameters
  pulse_scale_max: 110
  pulse_scale_normal: 100

# ============================================================================
# TEXT RENDERING SETTINGS
# ============================================================================
text_rendering:
  # Character width estimation factors
  narrow_char_width_factor: 0.4
  wide_char_width_factor: 1.2
  space_char_width_factor: 0.3

  # Text layout parameters
  max_text_width_percent: 0.8
  default_margin_fraction: 0.1
  default_font_size_scale: 1.0
  max_chars_per_line: 38

  # Subtitle duration limits
  max_subtitle_duration_sec: 4.5
  min_subtitle_duration_sec: 0.4

  # Safe positioning boundaries (fractions of frame)
  min_safe_y_position: 0.05
  max_safe_y_position: 0.95
  center_position_fraction: 0.5
  left_position_fraction: 0.1
  right_position_fraction: 0.9

  # Font size boundaries
  base_font_size_percent: 0.04
  min_font_size: 16
  max_font_size: 100

# ============================================================================
# SUBTITLE STYLE PRESETS
# ============================================================================
# Predefined style configurations with fonts, colors, and effects
style_presets:
  minimal:
    description: "Clean, simple styling with no effects"
    font_name: "Arial"
    font_color: "&H00FFFFFF"           # White text
    outline_color: "&H00000000"        # Black outline
    background_color: null             # No background
    bold: false
    outline_thickness: 1
    shadow: false
    effects: []
    font_width_to_height_ratio: 0.5

  modern:
    description: "Contemporary look with subtle effects"
    font_name: "Montserrat"
    font_color: "&H00FFFFFF"           # White text
    outline_color: "&H00000000"        # Black outline
    background_color: "&H99000000"     # Semi-transparent black background
    bold: true
    outline_thickness: 2
    shadow: true
    effects: ["karaoke"]
    font_width_to_height_ratio: 0.5

  bold:
    description: "High contrast, bold styling"
    font_name: "Gabarito"
    font_color: "&H00FFFFFF"           # White text
    outline_color: "&H00000000"        # Black outline
    background_color: "&HCC000000"     # Strong black background
    bold: true
    outline_thickness: 3
    shadow: true
    effects: ["fade"]
    font_width_to_height_ratio: 0.5

  animated:
    description: "Full animations and dynamic effects"
    font_name: "Gabarito"
    font_color: "&H00FFFFFF"           # White text
    outline_color: "&H00000000"        # Black outline
    background_color: "&H99000000"     # Semi-transparent black
    bold: true
    outline_thickness: 2
    shadow: true
    effects: ["movement"]
    font_width_to_height_ratio: 0.5

  random:
    description: "Random styling with one random effect per video"
    font_name: "Impact"                # Will be randomized
    font_color: "&H00FFFFFF"           # Will be randomized
    outline_color: "&H00000000"        # Will be randomized
    background_color: "&H99000000"
    bold: true
    outline_thickness: 2
    shadow: true
    effects: ["fade", "scale_pulse", "rotation_bounce", "glow", "typewriter", "karaoke", "movement"]
    font_width_to_height_ratio: 0.5

# ============================================================================
# SUBTITLE SEGMENTATION
# ============================================================================
subtitle_segmentation:
  # Word count thresholds
  min_words_for_sentence_break: 3
  min_words_natural_break: 3
  min_words_duration_limit: 3

  # Fallback timing
  fallback_segment_duration_sec: 2.5
